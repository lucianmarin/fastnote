# Fastnote

Note keeping and link sharing done with FastAPI and a single JSON file.

## Setup

- chmod +w /data/notes.json
- Generate MD5 of your password
- Set MD5 key as `PASSWORD_HASH` in `local.py`
- Login from /login
